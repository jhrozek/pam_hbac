MUST:
-----
* sasl bind

SHOULD:
-------
* handle multiple URIs
* SRV records
* devel mode that
  -- would not check perms on config file
  -- would print even authtok
  -- would be enabled at build time
* more unit tests
* trim attributes to download based on what we really need. The current set is copied from IPA
* parallelize lookups where possible
