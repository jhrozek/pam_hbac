# This is the most basic config file with
# a multi-line comment and five directives
URI = ldap://example.com
BASE = dc=example,dc=com
BIND_DN = uid=admin,cn=users,cn=accounts,dc=example,dc=com
BIND_PW = Secret1
CA_CERT = /path/to/file