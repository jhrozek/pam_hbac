# Tests that whitespace around the equal sign
# works OK
URI = ldap://example.com
BASE    =   dc=example,dc=com
BIND_DN = uid=admin,cn=users,cn=accounts,dc=example,dc=com
BIND_PW = Secret1
SSL_PATH = /path/to/file
